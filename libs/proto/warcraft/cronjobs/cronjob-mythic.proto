syntax = "proto3";

package warcraft.cronjobs;

import "warcraft/common.proto";

option go_package = "github.com/ToxicToast/Azkaban-Go/libs/proto/warcraft/cronjobs;cronjobs";

// 📌 Perioden-Info (z. B. Season oder aktuelle ID)
message PeriodRef {
  warcraft.Key key = 1;
  int64 id = 2;
}

// 📌 Aktuelle Rating-Daten
message CurrentMythicRating {
  warcraft.Color color = 1;
  float rating = 2;
}

// 📌 Mythic Keystone Profile
message KeystoneRating {
  warcraft.Self _links = 1;

  message CurrentPeriod {
    PeriodRef period = 1;
  }

  CurrentPeriod current_period = 2;
  repeated PeriodRef seasons = 3;

  warcraft.ShortCharacter character = 4;
  CurrentMythicRating current_mythic_rating = 5;
}