syntax = "proto3";

package warcraft.cronjobs;

import "warcraft/common.proto"; // Für Key, Self, Realm

option go_package = "github.com/ToxicToast/Azkaban-Go/libs/proto/warcraft/cronjobs;cronjobs";

// 📌 Bilddaten für Charaktermedien
message Asset {
    string key = 1;    // z. B. avatar, inset, main-raw
    string value = 2;  // URL zur Bilddatei
}

// 📌 Media-Antwort für einen Charakter
message Media {
    warcraft.Self _links = 1;
    warcraft.ShortCharacter character = 2;
    repeated Asset assets = 3;
}