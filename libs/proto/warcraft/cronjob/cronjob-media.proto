syntax = "proto3";

package warcraft;

import "warcraft/common.proto";

option go_package = "github.com/ToxicToast/Azkaban-Go/libs/proto/warcraft;warcraft";

// 📌 Bilddaten für Charaktermedien
message Asset {
    string key = 1;    // z. B. avatar, inset, main-raw
    string value = 2;  // URL zur Bilddatei
}

// 📌 Media-Antwort für einen Charakter
message Media {
    Self _links = 1;
    ShortCharacter character = 2;
    repeated Asset assets = 3;
}